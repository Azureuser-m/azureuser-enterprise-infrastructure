[defaults]
inventory = ./inventory.ini
remote_user = ansible
host_key_checking = True
retry_files_enabled = False 
roles_path = ./vendor/wazuh-ansible/roles/wazuh

[privilege_escalation]
become = True
become_method = sudo
become_user = root